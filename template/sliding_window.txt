//sliding window 

sort(nums.begin(), nums.end);
int left = 0, right = 0;
long res = 0, total = 0;
while(right < nums.size()){
    total += nums[right];

    while(nums[right] * (right-left+1) > total+k){ //invalid condition
        total -= nums[left];
        left++;
    }

    res = max(res, (right-left+1));
    right++;

    return int(res);
}